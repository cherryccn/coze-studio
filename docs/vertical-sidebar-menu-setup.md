# 垂直侧边导航菜单 - 启用指南

新的垂直侧边导航菜单已经成功集成到项目中！

## ✅ 已完成的工作

### 1. 创建的组件

#### 基础组件 (`@coze-foundation/space-ui-base`)
- ✅ `VerticalSidebarMenu` - 垂直侧边栏菜单基础组件
  - 位置: `frontend/packages/foundation/space-ui-base/src/components/vertical-sidebar-menu/index.tsx`

#### 适配器组件 (`@coze-foundation/space-ui-adapter`)
- ✅ `VerticalSidebarMenuAdapter` - 集成路由和状态管理的完整实现
  - 位置: `frontend/packages/foundation/space-ui-adapter/src/components/vertical-sidebar-menu-adapter/index.tsx`

### 2. 修改的文件

1. **国际化翻译**
   - `frontend/packages/arch/resources/studio-i18n-resource/src/locales/zh-CN.json`
   - `frontend/packages/arch/resources/studio-i18n-resource/src/locales/en.json`
   - 添加了所有新菜单项的中英文翻译

2. **主布局组件**
   - `frontend/packages/foundation/global-adapter/src/components/global-layout-composed/index.tsx`
   - 集成了新的垂直侧边栏菜单

3. **路由配置**
   - `frontend/apps/coze-studio/src/routes/index.tsx`
   - 添加了 "主页" 路由

4. **导出文件**
   - `frontend/packages/foundation/space-ui-base/src/index.tsx`
   - `frontend/packages/foundation/space-ui-adapter/src/index.tsx`

### 3. 菜单结构

**第一组 - 主要工作区：**
- 🏠 主页
- 🤖 项目开发
- 📚 资源库
- ✅ 任务中心
- 📊 效果评测
- ⚙️ 空间配置 🔴 (带橙色提示圆点)

**第二组 - 商店和社区：**
- 🏪 模板商店
- 🔌 插件商店
- 👥 作品社区

**第三组 - 管理和文档：**
- 🔗 API 管理
- 📖 文档中心
- 🛠️ 通用管理

## 🚀 如何启动

### 方式 1：使用开发服务器（推荐）

```bash
# 在项目根目录执行
cd frontend/apps/coze-studio
npm run dev
```

然后访问 http://localhost:3000

### 方式 2：使用 Docker 部署

```bash
# 在项目根目录执行
make web
```

然后访问 http://localhost:8888

## 📝 当前状态

新的垂直侧边栏菜单**默认已启用**。在 `global-layout-composed/index.tsx` 中：

```typescript
const USE_VERTICAL_SIDEBAR = true;  // 默认启用
```

### 如何切换回旧版本菜单

如果需要暂时切换回旧版本菜单，修改这一行为：

```typescript
const USE_VERTICAL_SIDEBAR = false;  // 使用旧版菜单
```

## 🔧 注意事项

### 1. 路由配置
新菜单中的路由已经配置好了，但部分页面可能需要创建：

- ✅ `/space/:space_id/home` - 已配置（暂时指向 develop）
- ✅ `/space/:space_id/develop` - 项目开发页面
- ✅ `/space/:space_id/library` - 资源库页面
- ⚠️ `/space/:space_id/task` - **需要创建任务中心页面**
- ⚠️ `/space/:space_id/test` - **需要创建效果评测页面**
- ⚠️ `/space/:space_id/config` - **需要创建空间配置页面**
- ⚠️ `/template` - **需要创建模板商店页面**
- ⚠️ `/plugin` - **需要创建插件商店页面**
- ⚠️ `/community` - **需要创建作品社区页面**
- ⚠️ `/api` - **需要创建API管理页面**
- ⚠️ `/doc` - **需要创建文档中心页面**
- ⚠️ `/management` - **需要创建通用管理页面**

### 2. 临时占位
学生相关的路由使用了临时占位组件（指向学习中心），可以在后续创建真实页面后更新。

### 3. 包依赖
确保以下包已正确构建：
- `@coze-foundation/space-ui-base`
- `@coze-foundation/space-ui-adapter`
- `@coze-foundation/global-adapter`

## 🎨 功能特性

✅ Logo + "扣子" 文字
✅ 空间切换下拉框（带搜索）
✅ "+ 创建" 按钮
✅ 三个视觉分组菜单
✅ 图标双态切换（默认/激活）
✅ 悬停背景变化
✅ 选中高亮效果
✅ 橙色提示圆点（空间配置）
✅ 完整国际化支持
✅ 响应式布局

## 📚 文档

详细的组件文档请查看：
- `frontend/packages/foundation/space-ui-base/src/components/vertical-sidebar-menu/README.md`

## 🐛 问题排查

### 如果菜单没有显示：

1. 检查是否在有侧边栏的页面（路由 loader 中 `hasSider: true`）
2. 检查 `USE_VERTICAL_SIDEBAR` 是否为 `true`
3. 检查浏览器控制台是否有错误
4. 清除浏览器缓存并重新加载

### 如果菜单项点击没反应：

1. 检查路由是否正确配置
2. 检查对应的页面组件是否存在
3. 查看浏览器控制台的网络请求

### 如果样式不正确：

1. 确保使用了 Coze Design System 的设计令牌
2. 检查 Tailwind CSS 配置是否正确
3. 尝试清除构建缓存：`rush build --clean`

## 🎯 下一步

1. **创建缺失的页面** - 为所有菜单项创建对应的页面组件
2. **完善创建功能** - 实现 "+ 创建" 按钮的下拉菜单
3. **添加通知系统** - 为橙色提示圆点添加动态通知
4. **测试响应式** - 在不同屏幕尺寸下测试菜单
5. **性能优化** - 优化菜单渲染性能

## 📞 技术支持

如有问题，请查看：
- [CLAUDE.md](/home/hjy/work/coze-studio/CLAUDE.md) - 项目开发指南
- [组件文档](/home/hjy/work/coze-studio/frontend/packages/foundation/space-ui-base/src/components/vertical-sidebar-menu/README.md)
